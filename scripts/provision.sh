#!/bin/bash
#
# This script represents the sort of state that might exist
# outside of the realm of your module's rspec test coverage
# (maybe even outside of your configuration management tool
# entirely)
#
# In summary - mess everything up
# iptables -I INPUT 1 -i lo -p tcp --dport 80 -j REJECT
